import 'package:flutter/material.dart';

import 'package:uas_sistem_aplikasi_lintas_platform/main_page.dart';

void main() => runApp(Uwg2App());

class Uwg2App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MainScreen(),
    );
  }
}
